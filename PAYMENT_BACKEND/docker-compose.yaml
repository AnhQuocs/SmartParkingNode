version: '3.8'

services:
  backend:
    build: .
    container_name: payment_backend
    ports:
      - "80:8080"
    environment:
      - SERVER_IPN_URL=http://174.138.23.196.nip.io/api/payment/momo-ipn
    restart: unless-stopped